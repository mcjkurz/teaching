---
layout: default
title: CHI3242 Assignment 0
---

<p class="updated i18n-block"><span class="lang-en">Last updated: Sep 17, 2026</span><span class="lang-zh" lang="zh-Hant">最後更新：2026年9月17日</span></p>
<p class="i18n-block"><a href="../"><span class="lang-en">Assignments</span><span class="lang-zh" lang="zh-Hant">作業</span></a> · <a href="../../"><span class="lang-en">CHI 3242 syllabus</span><span class="lang-zh" lang="zh-Hant">CHI 3242 課程大綱</span></a></p>
<h1 class="i18n-block"><span class="lang-en">Assignment 0 (not graded)</span><span class="lang-zh" lang="zh-Hant">作業0（不計分）</span></h1>

<div class="i18n-block">
<div class="lang-en">
<p><strong>Due:</strong> 23 Sep, 9:00 am</p>
<p>For this assignment, please complete the following steps:</p>
<ol>
<li>Create a new <strong>GitHub repository</strong> and open it in <strong>Codespaces</strong>.</li>
<li>Using the method taught in class, call the <strong>GLM-5.3-Flash</strong> model through <strong>OpenCode</strong> (with your own API key).</li>
<li>Choose a literary text you like and ask the model to suggest several ways of analyzing it, for example:
<ul>
<li>the most frequent words</li>
<li>words that characterize each figure in the text</li>
<li>dictionary-based emotion analysis: the distribution of positive and negative sentiment across the text</li>
</ul>
<p>The method can be very simple or somewhat more complex — that is up to you.</p>
</li>
<li>Once the analysis is finished, you will have a <code>.py</code> script, a <code>.txt</code> file, and an <code>.html</code> file presenting the final results.</li>
<li><strong>Commit</strong> and <strong>push</strong> all the files to your GitHub repository to save your work.</li>
</ol>

<h2>Publishing to GitHub Pages (three simple steps)</h2>
<p>So that your <code>.html</code> results can be viewed directly on the web, publish them as a GitHub Pages site:</p>
<ol>
<li>Make sure your final <code>.html</code> file is in the root of the repository and named <code>index.html</code>. (If it has a different name, remember to specify the correct file in the following steps.)</li>
<li>On your repository page, go to <strong>Settings</strong> → <strong>Pages</strong>.</li>
<li>Under <strong>Build and deployment</strong> → <strong>Source</strong>, choose:
<ul>
<li><strong>Branch:</strong> <code>main</code></li>
<li><strong>Folder:</strong> <code>/ (root)</code></li>
</ul>
<p>Then click <strong>Save</strong>. Wait 1–2 minutes and the site will be generated automatically.</p>
</li>
</ol>
<p>Once it is published, a URL will appear on the same page, in this format:</p>
<p><code>https://your-account.github.io/your-repo-name/</code></p>

<h2>Submission</h2>
<p>Please submit the following two links:</p>
<ul>
<li>the URL of your <strong>GitHub repository</strong></li>
<li>the URL of your <strong>GitHub Pages</strong> site (where your analysis can be viewed directly)</li>
</ul>
<p>If you have any questions, please feel free to ask!</p>
</div>
<div class="lang-zh" lang="zh-Hant">
<p><strong>截止：</strong>9月23日 上午9:00</p>
<p>在這次作業中，請完成以下步驟：</p>
<ol>
<li>建立新的 <strong>GitHub Repository</strong>，並在 <strong>Codespaces</strong> 中開啟。</li>
<li>依照課堂上教的方法，透過 <strong>OpenCode</strong> 呼叫 <strong>GLM-5.3-Flash</strong> 模型（使用你自己的專屬 API key）。</li>
<li>選擇一篇你喜歡的文學文本，請模型提出幾種分析方式的建議，例如：
<ul>
<li>最高頻詞彙</li>
<li>能代表每個人物特徵的詞語</li>
<li>基於詞典的情感分析（dictionary-based emotion analysis），分析文本中情感分佈（積極／消極）</li>
</ul>
<p>分析方法可以很簡單，也可以複雜一點，由你自己決定。</p>
</li>
<li>完成分析後，會產生 <code>.py</code> 腳本、<code>.txt</code> 檔案，以及最終呈現結果的 <code>.html</code> 檔案。</li>
<li>將所有檔案 <strong>commit</strong> 並 <strong>push</strong> 到你的 GitHub repo，保存這次的作業成果。</li>
</ol>

<h2>發布成 GitHub Pages 網頁（簡單三步）</h2>
<p>為了讓你的 <code>.html</code> 分析結果可以直接在網路上瀏覽，請照以下步驟把它發布成 GitHub Pages：</p>
<ol>
<li>確認你的最終 <code>.html</code> 檔案放在 repo 的根目錄下，並命名為 <code>index.html</code>（如果檔名不同，記得之後步驟要指定正確的檔案）。</li>
<li>到你的 repo 頁面，點選 <strong>Settings</strong> → <strong>Pages</strong>。</li>
<li>在 <strong>Build and deployment</strong> 底下的 <strong>Source</strong>，選擇：
<ul>
<li><strong>Branch:</strong> <code>main</code></li>
<li><strong>Folder:</strong> <code>/ (root)</code></li>
</ul>
<p>然後按 <strong>Save</strong>。等待 1–2 分鐘，網頁就會自動生成。</p>
</li>
</ol>
<p>發布成功後，你會在同一頁面上看到一個網址，格式如下：</p>
<p><code>https://你的帳號.github.io/你的repo名稱/</code></p>

<h2>提交方式</h2>
<p>提交作業時，請提供以下兩個連結：</p>
<ul>
<li><strong>GitHub Repository</strong> 的網址</li>
<li><strong>GitHub Pages</strong> 的網址（可以直接瀏覽你的分析結果的網頁）</li>
</ul>
<p>如有任何問題，歡迎隨時提問！</p>
</div>
</div>
